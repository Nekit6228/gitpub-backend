patch:
  tags:
    - User
  summary: Update user info
  description: Update name, email, gender, dueDate, avatar for authenticated user.
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/UserPatch.yaml'
  responses:
    '200':
      description: User updated
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/User.yaml'
    '400':
      $ref: '../../components/responses/400.yaml'
    '401':
      $ref: '../../components/responses/401.yaml'
